# 已更名为 GitHub Agent

原 **GitHub Actions 策略 v3** 已正式更名为 **GitHub Agent v3**。

新的规范入口：[`../github-agent/`](../github-agent/)

本目录暂时保留作为历史兼容入口，避免旧链接、旧提交说明和外部引用失效。现有模板/脚本文件名仍可能保留 `actions-*` / `actions_strategy_*` 兼容名称。
