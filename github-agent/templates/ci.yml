name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  fast-gate:
    name: Fast gate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Syntax, lint, compile, and focused contracts
        run: |
          echo 'Replace with fast deterministic checks.'

  full-test:
    name: Full test suite
    needs: fast-gate
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Run full tests
        run: |
          # Python: setup-python@v7, pytest-timeout, then full pytest.
          # Node: setup-node@v7, npm ci, then full tests.
          # Add path-aware heavy build jobs only after fast-gate succeeds.
          echo 'Replace with project full tests.'
